{
  "name": "dev-app",                   // App name (string)
  "version": "1.0.0",                        // Version of your app (string)
  "description": "A Linux-style web app",    // Description of your app (string)
  "main": "server.js",                       // Main entry file (string)
  "scripts": {
    "start": "node server.js",               // Start script for Render
    "dev": "nodemon server.js"                // Optional: for local development
  },
  "dependencies": {
    "express": "^4.17.1",                    // Express version (string)
    "body-parser": "^1.19.0"                  // Body parser version (string)
  },
  "devDependencies": {
    "nodemon": "^2.0.4"                      // Nodemon for development (string)
  }
}
